## SILEX CMS TEST
```
This is a simple CMS application
```

## Setup
```
After Downloading, run from your cmd
$ composer install

After that, create a .env file, and copy the content inside the .env-example inside

Next run the following two commands

$ php artisan migrate

$ php artisan db:seed

[Note: The default administrator password is; password]
```